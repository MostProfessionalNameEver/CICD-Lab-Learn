name: Secrets Demo

on:
  workflow_dispatch:

jobs:
  use-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Use the secret properly
        env:
          TOKEN: ${{ secrets.MY_SECRET_TOKEN }}
        run: echo "Token length is ${#TOKEN} characters"
